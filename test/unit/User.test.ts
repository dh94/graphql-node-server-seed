import { expect } from 'chai';
import { Logger } from '../../src/core/Logger';

describe('User model', () => {
	describe('aaaa', () => {
		it('should create new user', () => {
			Logger("check").info("sup");
			expect(true).to.be.true;
		});
	});
});
